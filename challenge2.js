function grader(grade) {
    let letter = "";
    if (grade >= 90) {
    return letter = "A for amazing!🤩 ";}
    else if (grade >= 80){
    return letter = "B is for Better than C!😀";}
    else if (grade >= 70)
    {return letter = "C is for Cool, you didn't fail!😌";}
    else if (grade >= 60)
    {return letter = "D is for Detention. 😔";}
    else;
    {return letter = "F. Oh no. 🤯";}
    
    
    
} 
console.log(grader(55))